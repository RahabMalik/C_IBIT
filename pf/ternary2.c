#include <stdio.h>
#include <math.h>
int main()
{
    int x, y, power, result;
    char op;
    printf("Enter an operator:");
    scanf("%c", &op);
    printf("Enter two operands:");
    scanf("%d %d", &x, &y);
    power = pow(x, y);
    (op=='+')

    return 0;
}