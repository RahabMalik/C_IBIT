#include <stdio.h>
int main()
{
    int arr[6] = {1, 2, 3, 4, 5, 6};
    int brr[6] = {7, 8, 9, 10, 11, 12};
    int crr[6];
    int i, j, swap;
    for (i = 0; i < 6; i++)
    {
        for (j = i + 1; j < 6; j++)
        {
            if (arr[i] && brr[i] < arr[j] && brr[j])
        }
    }

    return 0;
}