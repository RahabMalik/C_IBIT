#include<stdio.h>
#include<math.h>
int main()
{
    //calculate the square root of the number 
    double x,sq;
    printf(" Enter the number:");
    scanf("%lf",&x);
    sq = sqrt(x);
    printf("sq=%f",sq);
    return 0;
}